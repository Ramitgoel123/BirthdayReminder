// Here we will create the basic UI of the app such as the list and add birthdays etc
import React from 'react';
 // import data from "../data";
 import { View } from "react-native";

const Home = () => {
    return (
        <View> 
        
        

        {/* <TouchableOpacity onPress={() => this.props.navigation.navigate("addBirthdays") }>
        </TouchableOpacity> */}
        </View> 
      );
}

export default Home 